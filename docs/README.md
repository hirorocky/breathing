# ドキュメント

## 読む順番

1. **concept-v1/** — 現在の探求指針（v1.0.0 以降・StackChan・同室・勾配）
2. **journal/** — 観測版ごとの気づき（git タグと対応）
3. **concept/** — Web 探求期の指針（v0.1.0 / v0.2.0、参照用）
4. **../overlay/docs/my-cores3/** — StackChan 実機のビルド・MOD 開発ガイド

初めて読むときは、各フォルダの `README.md` から。

## バージョン

| 版 | 媒体 | 指針 |
|---|---|---|
| v0.1.0 / v0.2.0 | Web | `concept/` |
| **v1.0.0 以降** | StackChan | `concept-v1/` |

## 実装

- **upstream ファームウェア**: `stack-chan/`（git サブモジュール）
- **breathing 固有のパッチ・MOD・スクリプト**: `overlay/`
